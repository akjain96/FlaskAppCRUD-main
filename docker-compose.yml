version: '2'

services:
  web:
    build: .
    links:
      - db
    ports:
      - '8000:5000'
    networks:
      - default

  db:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      #MYSQL_ROOT_PASSWORD: rootpasswordgiven
      MYSQL_USER: root
      MYSQL_PASSWORD: rootpasswordgiven
      MYSQL_DATABASE: crud
    networks:
      - default